import React from 'react';
import styles from './home.module.scss';

const Home = () => (
  <div className="home">
    <main>
      <div>
        <h1 className={styles.title}>
          Start your project!
        </h1>
      </div>
    </main>
  </div>
);

export default Home;
